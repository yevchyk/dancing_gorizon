"""Horizon-conditioned CatBoost research pipeline.

This package is intentionally isolated from the existing fast/live model code.
The public entry points are the thin scripts:

    python -m src.run_hc_dataset
    python -m src.run_hc_train
    python -m src.run_hc_eval
"""

